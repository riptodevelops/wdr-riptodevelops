kind = "web"
previewPath = "/"
title = "OWLED Media - Creative Agency"
version = "1.0.0"
id = "artifacts/owled-media"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24436

[services.development]
run = "pnpm --filter @workspace/owled-media run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/owled-media", "run", "build" ]
publicDir = "artifacts/owled-media/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24436"
BASE_PATH = "/"
